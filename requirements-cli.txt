# Minimal dependencies for headless/TUI use (no PyQt6)

pywhispercpp>=1.4.0
numpy>=1.24.0
requests>=2.28.0

# Audio capture
sounddevice>=0.4.6

# VAD (preferred) + EnergyVAD fallback in code
webrtcvad-wheels>=2.0.0
